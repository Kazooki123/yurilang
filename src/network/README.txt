Networking from Scratch (YuriNET)

C written and uses windows header like <winsock2.h> but also POSIX compliant so if you use Linux compiling this to a dynamic library (`.so`) would be easy (i hope)

Note: Still very early on development, enough to integrate to Yurilang one day.